{% extends 'base.html.twig' %}

{% block title %}
    {{ 'label_data_change'|trans({'%id%': user.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'label_data_change'|trans({'%id%': user.id|default('')}) }}</h1>
    {{ form_start(form) }}
    {{ form_row(form.password) }}



    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'submit'|trans }}" class="btn btn-primary" />
    </div>

    {{ form_end(form) }}

{% endblock %}